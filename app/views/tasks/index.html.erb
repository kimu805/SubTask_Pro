<%= link_to "サブスク登録", new_subscription_path %>
<h1>Tasks#index</h1>
<p>Find me in app/views/tasks/index.html.erb</p>

<% @tasks.each do |task| %>
  <div>
    <%= task.title %>
    <%= task.description %>
    <%= task.deadline %>
    <%= task.priority.name %>
  </div>
<% end %>